function setup(){
    // Set up your drawing here
    createCanvas(500,500);
    background("beige");
    
    //fill(color(0,0,0)); //This is optional but maybe challenge if we are moving through this really fast
    circle(300,200,400);
}

function draw() {
    // Write your drawing code below
}
